# VDSR-Replication
This project aims to replicate the VDSR (Very Deep Super-Resolution) model in PyTorch. The model uses a 20-layer deep convolutional network to generate high-resolution images from low-resolution inputs.
